{
  "name": "bootstrap-stuff",
  "version": "1.0.0",
  "description": "bootstrap crash course",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "node-sass -w scss/ -o dist/css/ --recursive"
  },
  "author": "andrew",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^4.13.1"
  }
}
